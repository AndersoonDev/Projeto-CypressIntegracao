/// <reference types='cypress' />

describe('', () => {

    beforeEach(() => {
       cy.visit('https://wcaquino.me/cypress/componentes.html');
    });
    it('', () => {
        cy.get('#buttonDelay')
    });
});